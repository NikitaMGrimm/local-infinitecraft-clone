{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/target/debug/local-infinitecraft-clone",
            "args": ["banana",
                     "apple"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}